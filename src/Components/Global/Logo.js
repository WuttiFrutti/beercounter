import styled from 'styled-components';

export const Logo = styled.img({
    maxWidth: "80vw",
    maxHeight: "200px",
    objectFit: "contain",
    margin: "auto",
    marginTop: "2em",
    marginBottom: "2em"
})